<p style="color: green"><%= notice %></p>

<%= render @event %>

<div>
  <%= link_to "Edit this event", edit_app_event_path(@event) %> |
  <%= link_to "Back to events", app_events_path %>

  <%= button_to "Destroy this event", [:app, @event], method: :delete %>
</div>
