<%= form_with(model: [:app, threat_actor]) do |form| %>
  <% if threat_actor.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(threat_actor.errors.count, "error") %> prohibited this threat_actor from being saved:</h2>

      <ul>
        <% threat_actor.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :description, style: "display: block" %>
    <%= form.textarea :description %>
  </div>

  <div>
    <%= form.label :first_seen, style: "display: block" %>
    <%= form.datetime_field :first_seen %>
  </div>

  <div>
    <%= form.label :last_seen, style: "display: block" %>
    <%= form.datetime_field :last_seen %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
